{
  "name": "ci-cd-flow",
  "version": "1.0.0",
  "dependencies": {
    "@sap/cds": "^9"
  },
  "devDependencies": {
    "@cap-js/cds-test": "^1.0.1",
    "@cap-js/sqlite": "^2",
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@sap/cds-dk": "^10.0.4",
    "@sap/eslint-plugin-cds": "^4.2.4",
    "eslint": "^10.6.0",
    "husky": "^9.1.7",
    "jest": "^30.4.2",
    "lint-staged": "^17.0.8",
    "prettier": "^3.9.4"
  },
  "scripts": {
    "start": "cds-serve",
    "lint": "eslint .",
    "test": "jest --runInBand",
    "prepare": "husky"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/test/**/*.test.js"
    ],
    "testTimeout": 30000
  },
  "lint-staged": {
    "*.{js,mjs,cjs,cds}": "eslint --fix --no-warn-ignored",
    "*.{json,md,yaml,yml}": "prettier --write"
  },
  "private": true,
  "cds": {
    "requires": {
      "db-ext": {
        "[development]": {
          "model": "db/sqlite"
        },
        "[production]": {
          "model": "db/hana"
        }
      }
    }
  }
}
